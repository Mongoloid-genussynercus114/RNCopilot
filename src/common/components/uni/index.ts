import Ionicons from '@expo/vector-icons/Ionicons';
import { ActivityIndicator, Switch, TextInput } from 'react-native';
import { withUnistyles } from 'react-native-unistyles';

export const UniActivityIndicator = withUnistyles(ActivityIndicator);
export const UniSwitch = withUnistyles(Switch);
export const UniTextInput = withUnistyles(TextInput);
export const UniIonicons = withUnistyles(Ionicons);
