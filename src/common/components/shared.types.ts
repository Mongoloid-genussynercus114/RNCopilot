/** Semantic color scheme used across components for contextual styling. */
export type ColorScheme = 'primary' | 'success' | 'error' | 'warning' | 'info';

/** Standard component size scale used across the component library. */
export type ComponentSize = 'sm' | 'md' | 'lg';
